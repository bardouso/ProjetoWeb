import { Component } from '@angular/core'; import { Router, RouterModule } from '@angular/router'; import { FormsModule } from '@angular/forms'; @Component({ selector: 'app-home', standalone: true, imports: [RouterModule, FormsModule], templateUrl: './home.component.html', styleUrl: './home.component.css' }) export class HomeComponent { termoBusca = ''; constructor(private router: Router) { } buscar() { if (this.termoBusca.trim()) this.router.navigate(['/jogos'], { queryParams: { busca: this.termoBusca } }) } }
