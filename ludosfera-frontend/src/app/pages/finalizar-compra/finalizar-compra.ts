import { Component } from '@angular/core'; import { CommonModule } from '@angular/common'; import { FormsModule } from '@angular/forms'; import { Router } from '@angular/router'; import { ItemCesta } from '../../model/item-cesta'; import { PedidoService } from '../../services/pedido.service'; @Component({ selector: 'app-finalizar-compra', standalone: true, imports: [CommonModule, FormsModule], templateUrl: './finalizar-compra.html', styleUrl: './finalizar-compra.css' }) export class FinalizarCompra { lista: ItemCesta[] = []; total = 0; nomeCliente = ''; emailCliente = ''; enderecoEntrega = ''; formaPagamento = 'Pix'; mensagem = ''; constructor(private ps: PedidoService, private r: Router) { } ngOnInit() { let c = JSON.parse(localStorage.getItem('cliente') || '{}'); this.nomeCliente = c.nome || ''; this.emailCliente = c.email || ''; this.enderecoEntrega = c.logradouro || ''; this.lista = JSON.parse(localStorage.getItem('cesta') || '[]'); this.total = this.lista.reduce((s, i) => s + i.produto.preco * i.quantidade, 0) } finalizar() { const pedido = { nomeCliente: this.nomeCliente, emailCliente: this.emailCliente, enderecoEntrega: this.enderecoEntrega, formaPagamento: this.formaPagamento, itens: this.lista.map(i => ({ produtoId: i.produto.id, nomeProduto: i.produto.nome, quantidade: i.quantidade, precoUnitario: i.produto.preco })) }; this.ps.finalizarPedido(pedido).subscribe(res => { localStorage.removeItem('cesta'); alert('Pedido #' + res.pedidoId + ' finalizado!'); this.r.navigate(['/home']) }) } }
